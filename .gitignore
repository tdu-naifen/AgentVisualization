*.md
# Keep the corpus source markdown — the web build reads these to generate
# web/public/corpus.json.
!reference/shared/corpus/raw_markdown/*.md
node_modules
.superpowers
docs

# Next.js build artifacts
.next
out
*.tsbuildinfo
next-env.d.ts

# OS / editor
.DS_Store

# secrets — never commit
.env
.env.*
.next